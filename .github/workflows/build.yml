name: Build Android APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Клонируем репозиторий
        uses: actions/checkout@v4

      - name: 🏗️ Устанавливаем Python 3.12 и зависимости
        run: |
          sudo apt update
          sudo apt install -y software-properties-common
          sudo add-apt-repository ppa:deadsnakes/ppa -y
          sudo apt update
          sudo apt install -y python3.12 python3.12-venv openjdk-17-jdk zip unzip

      - name: 📦 Создаём виртуальное окружение и устанавливаем зависимости
        run: |
          python3.12 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip setuptools
          pip install buildozer Cython

      - name: 🔧 Создаём файл buildozer.spec
        run: |
          source venv/bin/activate
          buildozer init

      - name: 🔧 Создаём swap-файл (чтобы хватило памяти)
        run: |
          if [ ! -f /swapfile ]; then
            sudo fallocate -l 4G /swapfile
            sudo chmod 600 /swapfile
            sudo mkswap /swapfile
            sudo swapon /swapfile
            echo "SWAPFILE=/swapfile" >> $GITHUB_ENV
          fi

      - name: 📦 Устанавливаем Android SDK и принимаем лицензии
        run: |
          export ANDROID_SDK_ROOT=$HOME/.android-sdk
          mkdir -p $ANDROID_SDK_ROOT
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV

          if [ ! -d "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" ]; then
            echo "📥 Скачиваем Android Command Line Tools..."
            wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O android-sdk.zip
            unzip android-sdk.zip -d android-sdk
            mkdir -p $ANDROID_SDK_ROOT/cmdline-tools/latest
            mv android-sdk/* $ANDROID_SDK_ROOT/cmdline-tools/latest/
            rm android-sdk.zip
          fi

          export PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH
          echo "PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH" >> $GITHUB_ENV

          yes | sdkmanager --licenses
          sdkmanager --update
          sdkmanager "platform-tools" "platforms;android-34"

      - name: 🔍 Проверяем и устанавливаем Build-Tools, Aidl
        run: |
          install_build_tools() {
            version=$1
            echo "🛠️ Устанавливаем Build-Tools версии $version..."
            sdkmanager "build-tools;$version"

            if [ -f "$ANDROID_SDK_ROOT/build-tools/$version/aidl" ]; then
              echo "✅ Aidl найден в версии $version."
              return 0
            else
              echo "❌ Aidl не найден, пробуем другую версию..."
              return 1
            fi
          }

          # Пробуем версии Build-Tools
          if ! install_build_tools "34.0.0"; then
            if ! install_build_tools "33.0.0"; then
              if ! install_build_tools "30.0.3"; then
                echo "❌ Ошибка: Не удалось установить Build-Tools с Aidl."
                exit 1
              fi
            fi
          fi

      - name: 🔍 Проверяем и устанавливаем NDK
        run: |
          if [ ! -d "$ANDROID_SDK_ROOT/ndk/25.2.9519653" ]; then
            echo "📥 Устанавливаем NDK..."
            sdkmanager "ndk;25.2.9519653"
          fi

      - name: 📊 Проверка установленных версий
        run: |
          echo "🔹 Python:"
          python3.12 --version
          echo "🔹 Java:"
          java -version
          echo "🔹 Buildozer:"
          source venv/bin/activate
          buildozer --version
          echo "🔹 Android SDK:"
          sdkmanager --list_installed

      - name: 🚀 Сборка APK
        run: |
          source venv/bin/activate
          buildozer -v android debug

      - name: 📤 Загружаем APK в артефакты
        uses: actions/upload-artifact@v4
        with:
          name: AutoRegBot-APK
          path: bin/*.apk
