name: Build Android APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Клонируем репозиторий
        uses: actions/checkout@v4

      - name: 🏗️ Устанавливаем Python 3.12 и зависимости
        run: |
          sudo apt update
          sudo apt install -y software-properties-common
          sudo add-apt-repository ppa:deadsnakes/ppa -y
          sudo apt update
          sudo apt install -y python3.12 python3.12-venv openjdk-17-jdk

      - name: 📦 Создаём виртуальное окружение и устанавливаем зависимости
        run: |
          python3.12 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip setuptools
          pip install buildozer Cython

      - name: 🔧 Создаём файл buildozer.spec
        run: |
          source venv/bin/activate
          buildozer init

      - name: 🔧 Проверяем и удаляем существующий swap-файл
        run: |
          if swapon --show | grep -q '/swapfile'; then
            sudo swapoff /swapfile
            sudo rm -f /swapfile
          fi

      - name: 🔧 Создаём swap-файл (чтобы хватило памяти)
        run: |
          sudo fallocate -l 4G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          echo "SWAPFILE=/swapfile" >> $GITHUB_ENV

      - name: 📦 Устанавливаем Android SDK и принимаем лицензии
        run: |
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg

          # Скачиваем и извлекаем Android Command Line Tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O android-sdk.zip
          unzip android-sdk.zip -d cmdline-tools

          # Создаём правильную структуру директорий
          mkdir -p $ANDROID_SDK_ROOT/cmdline-tools/latest
          mv cmdline-tools/* $ANDROID_SDK_ROOT/cmdline-tools/latest/

          # Устанавливаем переменную окружения
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV

          # Принимаем лицензии
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses

          # Устанавливаем build-tools и platform-tools
          $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-34"

          # Функция для проверки наличия aidl
          check_aidl() {
            if [ -f "$ANDROID_SDK_ROOT/build-tools/$1/aidl" ]; then
              echo "✅ Aidl найден в версии $1."
              return 0
            else
              echo "❌ Aidl не найден в версии $1."
              return 1
            fi
          }

          # Устанавливаем Build-Tools и проверяем наличие aidl
          install_build_tools() {
            version=$1
            echo "🛠️ Устанавливаем Build-Tools версии $version..."
            $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager "build-tools;$version"

            if check_aidl "$version"; then
              echo "✅ Build-Tools $version установлены успешно."
              return 0
            else
              echo "❌ Build-Tools $version не установлены корректно."
              return 1
            fi
          }

          # Пробуем установить Build-Tools 34.0.0
          if ! install_build_tools "34.0.0"; then
            echo "🔄 Пробуем установить Build-Tools 33.0.0..."
            if ! install_build_tools "33.0.0"; then
              echo "🔄 Пробуем установить Build-Tools 30.0.3..."
              if ! install_build_tools "30.0.3"; then
                echo "❌ Не удалось установить Build-Tools. Проверьте логи."
                exit 1
              fi
            fi
          fi

      - name: 📦 Собираем APK
        run: |
          source venv/bin/activate
          buildozer -v android debug

      - name: 📤 Загружаем APK в артефакты
        uses: actions/upload-artifact@v4
        with:
          name: AutoRegBot-APK
          path: bin/*.apk
