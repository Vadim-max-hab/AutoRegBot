name: Build Android APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Клонируем репозиторий
        uses: actions/checkout@v4

      - name: 🏗️ Устанавливаем зависимости
        run: |
          sudo apt update
          sudo apt install -y openjdk-17-jdk
          pip3 install --upgrade pip

      - name: 📦 Устанавливаем Buildozer в виртуальном окружении
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install buildozer Cython==0.29.21

      - name: 🛠️ Увеличиваем память без swap
  run: |
    sudo mount -t tmpfs -o size=6G tmpfs /mnt

      - name: 📦 Устанавливаем Android SDK и принимаем лицензии
        run: |
          export ANDROID_HOME=$HOME/android-sdk
          export PATH=$ANDROID_HOME/cmdline-tools/latest/bin:$PATH
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg
          yes | sdkmanager --licenses
          sdkmanager --install "cmdline-tools;latest" "build-tools;34.0.0"

      - name: 📦 Собираем APK
        run: |
          source venv/bin/activate
          buildozer -v android debug

      - name: 📤 Загружаем APK в артефакты
        uses: actions/upload-artifact@v4
        with:
          name: AutoRegBot-APK
          path: bin/*.apk
