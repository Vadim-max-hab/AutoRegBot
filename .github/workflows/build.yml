name: Build Android APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Клонирование репозитория
        uses: actions/checkout@v4

      - name: 🏗️ Установка Python 3.12 и зависимостей
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository ppa:deadsnakes/ppa -y
          sudo apt-get update
          sudo apt-get install -y python3.12 python3.12-venv openjdk-17-jdk unzip wget

      - name: 📦 Создание виртуального окружения и установка зависимостей
        run: |
          python3.12 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip wheel setuptools
          pip install buildozer==1.7.1 Cython==0.29.33

      - name: 🔧 Инициализация buildozer.
